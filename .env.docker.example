# Local container runtime env only.
# Use the Docker host alias so the app container can reach PostgreSQL running on the host machine.

DATABASE_URL=postgresql://postgres:postgres@host.docker.internal:5432/wshka
DATABASE_SSL=false
