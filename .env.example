# Local runtime env only. Do not commit real secrets.

# Required: PostgreSQL connection string for app runtime and Drizzle CLI.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/wshka

# Optional: set to true, 1, or yes when PostgreSQL SSL is required.
DATABASE_SSL=false
